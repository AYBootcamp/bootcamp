
/**
 * Given any dob, calculates the current age.
 * return -1 if dob is invalid or at a future date.
 * For example: calculateAge("1994-04-05") should return 28
 * @param {string} dob date of birth string. Format: YYYY-MM-DD
 */
function calculateAge(dob) {
    return -1
}

export default calculateAge;