
/**
 * Given 2 array of objects, find objects that are exactly the same
 * return array of all objects that are matching
 * otherwise return undefined
 * For example: 
 * @param {object[]} ary1 
 * @param {object[]} ary2 
 * @returns findMatch([{a:1}], [{a:1}]) should return [{a:1}]
 */
function findMatch(ary1, ary2) {
    return undefined
}

export default findMatch;